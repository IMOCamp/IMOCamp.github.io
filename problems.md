## **A0**
$x_1 \ge x_2 \ge \cdots \ge x_n > 0$ 且 $x_1x_2\cdots x_n=1.$
求最大的正實數 $C$，使得對所有滿足上述條件的 $(x_1,x_2,\dots,x_n)$，皆有：
\[
\sum_{i=1}^n x_i^2 - n \ge C(x_1-x_n)^2
\]

## **A1**
設 $a,b,c,d$ 為大於 $1$ 的實數，且滿足
\[
a+b+c+d=8
\]
求下列表達式的最小值：
\[
\frac{a}{(b+c)^2-1}+\frac{b}{(c+d)^2-1}+\frac{c}{(d+a)^2-1}+\frac{d}{(a+b)^2-1}
\]

## **A2**
設 $n$ 為正整數。令 $P(x,y,z)$ 為一個三元多項式，並假設對所有實數 $a$，以下式子皆成立
\[
P\bigl(a^n,\ a^{n+1},\ a^{n+2}+a\bigr)=a
\]
若定義
\[
Q(x)=P(x,-1,0),
\]
證明多項式 $Q(x)$ 的次數至少為 $n$。

## **A3**

實數 $a_1,a_2,\ldots,a_{2026},a_{2027},a_{2028},a_{2027}=a_1,a_{2028}=a_{2}$，找到最小的正實數 $C$ 使得以下式子恆成立
\[
C\sum_{k=1}^{2026}a_k(a_k-a_{k+1}) \geq \sum_{k=1}^{2026}a_k(2a_{k+1}-a_k-a_{k+2})
\]

## **C0**
平面上有 $n$ 個人，其位置可視為 $n$ 個點，且任意兩點之間的距離皆互不相同。現在每個人同時向距離自己最遠的那個人開槍。若一個人被至少一人射中，則視為死亡；否則視為存活。
對於哪些正整數 $n$，可以確定一定有人存活。

## **C1**
給定一個 $2026\times 2026$ 的方格表，其中藏著一隻隱形的兔子。每天依序進行以下兩個步驟：
1. 兔子必須移動到一個與目前所在格子有公共邊的相鄰格子；
2. 獵人選擇恰好 $2025$ 個格子進行搜尋。若兔子位於其中任一格，則獵人獲勝。
假設獵人在任何時刻都看不見兔子的確切位置，但可以根據先前的搜尋結果決定之後的策略。
問：兔子是否存在一種策略，使其無論獵人如何搜尋，都能永遠不被抓到？

註：做出獵人選擇恰好 $2026$ 個格子進行搜尋的情況會有部份分。

## **C2**
設 $n$ 為正整數，$P$ 為一個凸 $(2n+1)$ 邊形。若 $P$ 的一個頂點 $v$ 滿足：存在一條通過 $v$ 的直線，將 $P$ 分成兩個多邊形，且這兩個多邊形的頂點數相同、周長也相同，則稱 $v$ 為 $P$ 的一個**天秤點**。

對固定的 $n$，求當 $P$ 遍歷所有凸 $(2n+1)$ 邊形時，$P$ 的天秤點個數所有可能的值。

## **C3**

給定正整數 $n$。考慮由字元 $\{1,2,3\}$ 所組成的長度為 $n$ 的字串
\[
s=s_1s_2\cdots s_n \qquad (s_i\in\{1,2,3\}).
\]
定義一次操作如下：若對某個 $1\le i\le n-1$ 有 $s_i=s_{i+1}$，則可以將相鄰的兩個相同字元依下列規則替換：
\[
11\to 23,\qquad 22\to 13,\qquad 33\to 12,
\]

對任意初始字串 $s$，證明操作總會結束。


## **G0**
給定三角形 $ABC$ 及其外接圓 $\Gamma$，$AB \lt AC$，$H, O$ 分別是 $ABC$ 的垂心和外心，設 $X$ 是 $AO$ 和 $BC$ 交點，設 $AHX$ 外接圓和 $BC, \Gamma$ 再交於 $P, Q$，證明 $PQ$ 垂直 $AO$。

## **G1**
給定三角形 $ABC$ 及其外接圓 $\Gamma$，$\angle A=60^\circ$，$I$ 為 $ABC$ 內心，$K$ 是 $BIC$ 垂心，$BI, CI$ 交 $\Gamma$ 於 $E, F$，證明 $AK, EF, BC$ 三線共點。

## **G2**
給定三角形 $ABC$，其中 $AB \lt AC$，$H, O$ 為 $ABC$ 垂心和外心，考慮 $T$ 使得 $BHTC$ 是等腰梯形，令 $AT$ 和 $BHC$ 外接圓再交於 $Q$，考慮 $QH$ 上一點滿足$\measuredangle BAP=\measuredangle QAC$，證明 $BPC$ 的外角平分線垂直於 $AO$。

## **G3**
給定三角形 $ABC$，$H$ 是 $A$ 對 $BC$ 垂足，$I$ 是內心，$I_A$ 表示 $A-$旁心，其餘同理，要證明存在點 $\textrm{David}$ 滿足以下條件：
1. $\textrm{David} \in (AIH)\cap (I_AI_BI_C)$
2. 過 $\textrm{David}$ 垂直 $BC$ 的直線、過 $\textrm{David}, B$ 且圓心在 $AB$ 的圓、過 $\textrm{David}, C$ 且圓心在 $AC$ 的圓共點。



## **N0**

我們說一個正整數 $a$ 是好數如果 $S(a^n-1)$ 是嚴格遞增的，反之則是壞數，其中 $S(x)$ 表示把 $x$ 寫成十進制後數碼的和，證明好數壞數都有無限多個。

## **N1**

當正整數 $n$ 充分大時，對任意整數 $T$，若
\[
T > 2.026 n^3,
\]
證明存在正整數 $a,b,c$ 使得
\[
an^2+bn(n+1)+c(n+1)^2=T.
\]

## **N2**

對於每個整數 $N \ge 2$，定義數列 $\{a_n\}$ 如下：
\[
a_1 = N, \qquad a_{n+1} = a_n - f(a_n),
\]
其中 $f(m)$ 表示 $m$ 的最大質因數。設 $T(N)$ 為最小的正整數 $t$，使得 $a_t = 0$。

證明：存在整數 $N_0$，使得當 $N \ge N_0$ 時，
\[
T(N) \lt \frac{N}{2026}.
\]

## **N3**

請證明若 $a_1 \lt a_2 \lt a_3$，則正整數數列 $a_{n+2}=a_{n+1}^n-a_{n-1}$ 的質因數集合無限。
